# UrbanLaw 5.18 발표자료 — 핸드오프 (2026-05-15)

새 Claude Code 세션에서 이 작업을 이어가려면 **이 파일을 먼저 읽어주세요.**
사용자(영빈)가 발표 직전까지 슬라이드 카피·레이아웃 수정 요청을 빠르게 던지는 흐름입니다.

---

## 작업 컨텍스트

- **작업 대상 파일**: `/Users/youngbin/Downloads/urbanlaw-518/index.html` (단일 standalone HTML 발표자료, 1920×1080 deck)
- **저장소**: 같은 디렉토리에 git 초기화 — `origin = https://github.com/hozazak/urbanlaw-518.git` (main 브랜치)
- **발표일**: 2026-05-18
- **사용자**: 도시조경본부 도시설계팀 영빈 (qw6165@naver.com). UrbanLaw(어반로우) 자체 법규 검토 AI 개발 사례 발표.
- **현재 최신 커밋**: `2edbb37 p19: 카드 04 '법규 최신성 유지 프로세스 개발' 추가, 4열 그리드로 전환`

## ⚠️ 파일 구조 — 매우 중요

이 파일은 한 차례 큰 변환을 거쳤음:

1. **2026-05-14 09:56 이전**: Claude Design "bundled" 형태였음. `<script type="__bundler/template">` 안에 전체 deck HTML이 **JSON 문자열로 인코딩**되어 들어 있었고, runtime이 풀어서 렌더링.
2. **2026-05-14 09:56 이후**: 사용자의 "Local HTML Live Studio" 도구가 unbundle 처리해서, 지금은 **일반 expanded HTML**. `<deck-stage>` web component + `<section>` 직접 children 구조.
3. 따라서 **현재는 그냥 Edit 툴로 직접 HTML 편집하면 됨.** 과거 JSON re-encode 같은 트릭 불필요.

오래된 백업 (`index.bundle-backup-20260514095659.html`, `index.original-backup.html`, `index.html.bak-*`)이 잔뜩 있음. 손대지 말 것.

## 슬라이드 구조

20장 deck. `<section data-screen-label="NN 라벨">` 형식. 1-indexed.

| #  | 라벨 |
|----|------|
| 01 | 표지 |
| 02 | 회사가 지원한 AI 자원 |
| 03 | 활용 결과 · 명암 |
| 04 | Gemini 이미지 활용 사례 |
| 05 | 출발점이 되지 못하는 답 (GPT 법규 답변 한계) |
| 06 | GPT는 정직하게 모른다고 답합니다 |
| 07 | 왜 GPT는 안 통했나 |
| 08 | 회사 시스템과 새로 필요한 것 |
| 09 | 그래서 — 어반로우 (dark) |
| 10 | GPT가 못한 4가지를 자체 AI로 해냈습니다 (dark) |
| 11 | 주소 한 줄 · 다양한 토지정보 한곳에 |
| 12 | 어반로우 작동 원리 (GPT vs UrbanLaw 비교) |
| 13 | 지식 엔지니어링 (꼬리표 시각화) |
| 14 | 직접 사용하는 모습 (시연 영상) |
| 15 | 비교 사례 1 (마포 동교동 관광숙박) |
| 16 | 비교 사례 2 |
| 17 | 비교 사례 3 (청담 소규모재건축) |
| 18 | 종합 평가 (역량 비교 막대그래프) |
| 19 | 향후 계획 (4 카드 로드맵) |
| 20 | 감사합니다 (dark) |

`data-screen-label`은 단순 라벨이지만 일부 CSS가 `section[data-screen-label^="NN "]` 셀렉터로 슬라이드별 스타일을 거는 데 씀. 라벨 절대 바꾸지 말 것.

## 핵심 디자인 토큰 / CSS 패턴

`<section>` 기본:
- 1920×1080 fixed, padding 90/110/110/110
- flex column, overflow:hidden
- `.head` (top, kicker + meta 19/20) + 콘텐츠 + `.foot` (absolute bottom:50px left/right:110px)

색상 토큰 (CSS variables):
- `--paper`: #F4EFE6 (배경)
- `--ink`: navy-ish 진한 글씨
- `--navy` / `--teal` / `--teal-light` / `--teal-2` / `--gold` / `--warn`(orange #B83A14)
- `.dark` 섹션은 navy bg + paper 글씨

애니메이션 패턴 (매우 중요):
```css
.anim{ opacity: 0; }
section.active .anim{ animation: riseIn 0.85s ... both; }
section.active .anim.fade{ animation-name: fadeIn; }
.d1{ animation-delay: 0.05s !important; }
.d2{ animation-delay: 0.18s !important; }
... up to d9 등
```
- 슬라이드에 진입(`section.active`)하면 .anim 자식들이 d1, d2... 순서로 등장.
- **새 요소를 추가할 때 슬라이드 진입과 함께 페이드인되어야 한다면 `class="anim fade dN"` 추가.**
- 안 그러면 비활성 슬라이드일 때 잔상으로 보임 (p10 dashed line 사례).
- p18 막대그래프는 `.p18-bar-fill` + `--bar-target: XX%` 커스텀 패턴으로 0→목표 width 애니메이션 (별도 keyframe `p18BarFill`).

## 사용자 작업 스타일

- 발표 임박해서 빠르고 자잘한 카피·레이아웃 수정 요청을 던짐. 한국어 짧게.
- "N페이지" / "Np" = 슬라이드 1-indexed (= `data-screen-label` 앞 숫자).
- 직설적 스타일. "딱 맞게", "꽉 차게", "빼줘", "강조해줘", "줄바꿈" 등.
- 메모 패턴: 박스가 작아 보이면 **폰트 줄이지 말고** 박스를 키우거나 padding 늘릴 것 (이전 피드백).
- "구분이 잘 되게" → 색·크기 대비 강화. UrbanLaw는 강조(teal), GPT는 디엠퍼시스(다크/베이지).
- 영상/SVG가 푸터를 침범하면 max-height로 캐핑. p14 영상 자체는 1206×1080 (세로형, aspect 1.117).

## 매번 따르는 워크플로우

1. 사용자 요청 받기
2. 영향 받는 슬라이드 위치 파악 (section 찾기 → 컨텐츠 읽기)
3. Edit 또는 Bash + Python sed-like replace
4. **반드시** `git add index.html && git commit -m "..." && git push` 까지. 사용자가 매번 푸시까지 기대함.
5. 짧게 한국어로 결과 보고 (커밋 해시 범위 포함).

커밋 메시지 컨벤션: `pN: <짧은 설명>` 또는 `pN, pM: <설명>`. Co-Authored-By 같은 trailer 안 씀 (사용자 git config 미설정 상태라 author warning 나오지만 무시).

git config 변경하지 말 것 (사용자 설정 안 했음, 매 커밋마다 warning 나오지만 정상).

## 백업 정책

크게 손대는 변경(레이아웃 재구성 등) 전에는 `cp index.html index.html.bak-$(date +%Y%m%d-%H%M%S)-<reason>` 으로 타임스탬프 백업. 작은 카피 수정은 git으로 충분.

## 메모리 / 휘발성 주의사항

- 사용자가 active_memory_plugin로 컨텍스트 힌트를 줄 수 있음 — 신뢰성은 보통이지만 참고용.
- "법규" 관련 실제 조문 인용은 반드시 WebSearch로 검증할 것 (p13 사례: §55가 용적률인 줄 알았는데 사실 건폐율이었음. 사용자가 "본문 제대로 확인" 요청해서 정정함). 법령 인용 시 약칭 "영"은 본문 원문에서는 그대로, 보조 라벨에는 "시행령"으로 풀어 쓰는 패턴 유지.

## 참고: 최근 커밋 30개

```
2edbb37 p19: 카드 04 '법규 최신성 유지 프로세스 개발' 추가, 4열 그리드로 전환
af51ada p18: 하단 카피에 '완성이 아닌,' 강조 추가
741f492 p14: 단계 05 'PDF로 바로 보고서 출력' 추가
8b878f0 p8: '우리 업무와의 갭'에서 '자치 조례' 제거
e60746f p11: '규제 9종' → '규제'
686abed p12: GPT 검색바 깜박이 커서 제거
7259281 p15: FLOW FROM 09 → 14
9197ff2 p8: '새로운 도구가 필요했습니다' 줄바꿈
ab5ea21 p4: '보고서에 바로 쓸 수 있는 수준' 문구 제거
4e441d4 p3: 동일 문구 제거
d5b0b79 p10: 점선 SVG에 anim fade d6 적용
2fe7684 p13: §55 본문 정정 — 건폐율 조문
494fe76 p13: 꼬리표를 실제 조문으로 교체
58ab990 p18: 막대그래프 채우기 애니메이션 추가
e365f23 p12: UrbanLaw 박스 강조 (teal 그라데이션·테두리·그림자)
cb4108a p12: pill 폰트 확대 (13→22), GPT 다크 톤
55ae550 p5: 박스 높이를 글자 끝에 맞춤
b973fd1 p5: max-width 1500, stretch
5dbee1f p9, p20: 'UrbanLaw v2' → 'UrbanLaw'
ea5af59 p5: '다섯 군데' → '직접 검색하며 확인 필요'
4e75c8c p14: 헤드라인 '검토까지 5-10분'
c852e4f p14: 80초 → 빨리감기 + 실제 5-10분
02d2924 p18: AI 한계선 가시성 강화
0549b16 p14: 영상 양옆 검정 여백 제거
faa6fa2 p14: 영상 max-height 640px
fd2b34f p18: 헤드라인 '판단' 중복 제거 — '정성적 판단·리스크·결론'
0d7c013 p18: 하단 카피 '계속 만들어가는 과정' 1줄 통일·확대
bdcaee6 p18: 단일 통합 차트 + '계속 만들어가는 과정' 강조 복원
7896b64 p18: 종합평가 레이아웃을 역량 비교 막대그래프로 재구성
4cd7455 p18: 강조 블록 승격, 90%/5-10분 박스 축소
```

## 새 세션 시작 시 첫 메시지 예시

> "Downloads/urbanlaw-518/HANDOFF-2026-05-15.md 먼저 읽고 컨텍스트 파악해줘. 이어서 작업 진행할게."
